{
  "name": "cms22-api-server-schema",
  "version": "0.6.2",
  "description": "OpenAPI schema for cms22-api-server",
  "scripts": {
    "lint:spectral": "spectral lint ./api.yml -F hint",
    "lint:prettier": "prettier --check .",
    "lint": "run-p lint:*",
    "build:doc": "redoc-cli bundle ./api.yml"
  },
  "author": "yuseiito",
  "license": "(MIT OR Apache-2.0)",
  "devDependencies": {
    "@stoplight/spectral": "^5.8.1",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.2.1",
    "redoc-cli": "^0.10.2"
  }
}
